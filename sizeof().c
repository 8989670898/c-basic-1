#include<stdio.h>
main()
{
    char c;
    short s;
    printf("Size of the character: %d bytes(s)\n", sizeof(c));
    printf("Size of the short: %d bytes(s)\n", sizeof(s));
    printf("Size of the float: %d bytes(s)\n", sizeof(float));
}
